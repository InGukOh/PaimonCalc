<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paimonwillcalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_paimoncalc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_paimoncalc_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PaimonWillCalc">

        <activity
            android:name=".page01"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".page02"
            android:launchMode="singleTop"/>
        <activity android:name=".page03"
            android:launchMode="singleTop"/>
        <activity android:name=".page04"
            android:launchMode="singleTop"/>
        <activity android:name=".page05"
            android:launchMode="singleTop"/>
    </application>

</manifest>